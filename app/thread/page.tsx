import { Stub } from "@/components/Stub";

export default function ThreadPage() {
  return (
    <Stub
      eyebrow="The built experience"
      title="A + B's hero + D's verbs"
      note="Five states: the Sources rail with provenance and recurrence; highlight-to-note; the right-click verb set; multi-select and ask-within; and the set becoming a collection with everything intact."
      slice="Slice 4"
    />
  );
}
