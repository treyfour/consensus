import { Stub } from "@/components/Stub";

export default function DeckPage() {
  return (
    <Stub
      eyebrow="Presentation"
      title="Problem, process, directions, concept"
      note="Ten sections: framing, process, the problem defined with evidence and four pain points, who this is for, the landscape, four directions, the concept, key states, tradeoffs, and what I cut."
      slice="Slice 5"
    />
  );
}
